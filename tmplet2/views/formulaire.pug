<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title inscription
    body
        form(action="formulaire.pug" style="color:orange;background:#bababa")
            p inscription
            input(type="text" placeholder="tapez le nom" name="nom" style="color:orange;background:#bababa")
            input(type="submit", value="envoi" style="color:orange;background:#bababa")
            a(href="http://facebook.com") conecton facebouk
            script
                include message.js